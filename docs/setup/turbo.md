## Installing Turbo + Stimulus

This project uses Hotwire (Turbo + Stimulus) to build a fast and interactive UI without a full SPA framework.

- Install via npm  
`npm install @hotwired/turbo @hotwired/stimulus`  

- Project launch  
`npm run dev`
